<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Home{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
    {% block navigation %}
        <nav>
            <ul>
                <li><a href="{{ path('login') }}">login</a></li>
                <li><a href="{{ path('register') }}">registrer</a></li>
                <li><a href="{{ path('creation_post') }}">create Article</a></li>
            </ul>
        </nav>
    {% endblock %}

    {% block body %}{% endblock %}
    </body>
</html>
